{
  "name": "typescript-readibility-native",
  "version": "0.1.1",
  "description": "Blazing fast TypeScript readability powered by Rust (dom_smoothie + napi-rs)",
  "license": "MIT",
  "main": "js/wrapper.js",
  "types": "js/wrapper.d.ts",
  "type": "commonjs",
  "scripts": {
    "build": "npm run build:native",
    "build:native": "npx napi build --platform --release --cargo-cwd native --js native/index.js --dts native/index.d.ts",
    "test": "npm run build -- && vitest run",
    "test:watch": "vitest watch"
  },
  "napi": {
    "name": "readibility_native",
    "triples": {}
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0",
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  }
}
